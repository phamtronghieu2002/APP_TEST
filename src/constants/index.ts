import { FilterList } from "@/components/Filter.tsx/Filter";

export const filterList: FilterList[] = [

    { label: "role", options: [{ label: "Admin", value: "admin" }, { label: "User", value: "user" },{
        label:"Moderator",
        value:"moderator"
    }] },
  ];

  